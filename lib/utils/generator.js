'use strict';

import _debug   from 'debug';

const debug = _debug('lark-CLI');

const generator = {
    version     : null,
    application (appname) {

    },
    controller (route) {

    },
    page (route) {

    },
    api (route) {

    },
    data (route) {

    },
    dao (route) {

    },
    lib (route) {

    },
    template (route) {

    },
};

debug("lib/utils/generator.js load");
export default generator;
